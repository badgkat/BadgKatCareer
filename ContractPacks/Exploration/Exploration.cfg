// BadgKat Exploration — Two-track exploration system
// Probes scout first (cheaper, easier). Crew follows (harder, more rewarding).
// Replaces ExplorationPlus with a system that gives both tracks purpose.

CONTRACT_GROUP
{
	name = BadgKatExploration
	displayName = Exploration
	agent = Exploration

	tip = Exploring the unknown...

	minVersion = 1.15.0

	// Disable stock exploration since we replace it
	disabledContractType = ExplorationContract
}
